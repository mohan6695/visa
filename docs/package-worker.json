{
  "name": "rag-chatbot-worker",
  "version": "1.0.0",
  "description": "RAG chatbot using Meilisearch and Groq",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js --external:cloudflare --platform=node"
  },
  "dependencies": {
    "wrangler": "^3.72.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250114.1",
    "esbuild": "^0.21.0",
    "typescript": "^5.3.3"
  }
}
